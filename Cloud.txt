ls ~/.ssh
ssh-keygen -t ed25519
(enter 2-3 times)

eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub

ssh -T git@github.com

git clone <SSH Git >




go to BUN website
copy the Linux commands and paste in terminal

curl -fsSL https://bun.sh/install | bash


source /home/ubuntu/.bashrc

cd s3_cloudfront_simple_integration/backend
 bun i

 cp .env.example .env

nano .env
paste URL

create bucket
create IAM/user 
and paste secret key and key in .env

sudo npm install -g pm2
 pm2 start "bun start" --name "Node Cloud"

cd /etc/apache2/sites-available
sudo cp 000-default.conf navi-vvce.conf

sudo nano navi-vvce.conf

ProxyRequests Off
        ProxyPreserveHost On
        ProxyVia Full
        <Proxy *>
                Require all granted
        </Proxy>

        ProxyPass / http://127.0.0.1:3000/
        ProxyPassReverse / http://127.0.0.1:3000/


sudo a2enmod proxy
sudo a2enmod proxy_http

sudo systemctl reload apache2
sudo snap install --classic certbot
sudo certbot --apache
